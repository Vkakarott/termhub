// Placeholder entry point — Task 13 replaces this with the real CLI (login/run/status commands).
import { AGENT_VERSION } from './version.js';

console.log(`termhub-agent ${AGENT_VERSION}`);
process.exit(0);
