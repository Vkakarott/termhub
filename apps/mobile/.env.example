# Server the app talks to. Production builds bake https://termhub.dev in through eas.json;
# this file only matters for `expo start` against a local or staging server.
# The mobile API lives on the host without Cloudflare Access (spec §12.1).
EXPO_PUBLIC_TERMHUB_URL=http://localhost:3000
