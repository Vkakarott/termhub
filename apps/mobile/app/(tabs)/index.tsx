import { Placeholder } from '@/ui/placeholder';

/** Chats (spec §11.2): Chat geral and the projects, each with "respondendo" and a pending badge. */
export default function ChatsScreen() {
  return <Placeholder title="Chats" hint="O chat geral e um chat por projeto, com o que cada um está esperando de você." />;
}
