import { Placeholder } from '@/ui/placeholder';

/** Ajustes (spec §11.2): this device, biometrics switch, chat host, "Sair e remover este aparelho". */
export default function SettingsScreen() {
  return <Placeholder title="Ajustes" hint="Este aparelho, biometria, a máquina e a conta que hospedam o chat, e sair removendo o aparelho." />;
}
