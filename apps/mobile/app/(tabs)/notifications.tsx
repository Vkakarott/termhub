import { Placeholder } from '@/ui/placeholder';

/** Notificações (spec §9): the in-app history of every push sent to this account. */
export default function NotificationsScreen() {
  return <Placeholder title="Notificações" hint="Tudo o que o termhub avisou: ações esperando confirmação, respostas prontas e aparelhos novos." />;
}
